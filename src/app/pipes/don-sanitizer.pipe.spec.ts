import { DonSanitizerPipe } from './don-sanitizer.pipe';

describe('DonSanitizerPipe', () => {
  it('create an instance', () => {
    const pipe = new DonSanitizerPipe();
    expect(pipe).toBeTruthy();
  });
});
